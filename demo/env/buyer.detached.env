# Buyer: detached-mode rehearsal. Dev placeholders only — never put real secrets
# in this file; real env files (demo/env/*.mainnet.env) and keys (demo/env/keys/)
# are gitignored. Detached mode never reaches post-signing transaction
# processing, so a dummy RPC is fine.
# Usage (from the repo root):
#   source demo/env/buyer.detached.env && npm run demo:legacy:buyer
export SUBLY_ADMIN_API_TOKEN=dev-admin
export SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
export SUBLY_DEMO_AGENT_KEYPAIR_PATH=demo/env/keys/agent-detached.json
export SUBLY_FACILITATOR_URL=http://localhost:3000
