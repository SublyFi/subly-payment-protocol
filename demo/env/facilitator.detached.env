# Facilitator/relayer: detached mode (no DB, RPC, or sponsor key needed; no
# funds move). Dev placeholder tokens only — never put real secrets in this file.
# Usage (from the repo root):
#   source demo/env/facilitator.detached.env && npm run dev
export NODE_ENV=development
export SUBLY_SELLER_API_TOKEN=dev-seller
export SUBLY_ADMIN_API_TOKEN=dev-admin
