# Seller: detached-mode rehearsal. The payout wallet below is a throwaway demo
# pubkey (any pubkey works as the payout wallet in detached mode). Dev
# placeholders only — never put real secrets in this file.
# Usage (from the repo root):
#   source demo/env/seller.detached.env && npm run demo:legacy:seller
export SUBLY_SELLER_API_TOKEN=dev-seller
export SUBLY_DEMO_SELLER_WALLET=G9rwPde4LPjkMBf8WApbMa4qCEwBbwyRa67uJU6PLyUP
